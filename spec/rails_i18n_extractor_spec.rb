# frozen_string_literal: true

RSpec.describe RailsI18nExtractor do
  it "has a version number" do
    expect(RailsI18nExtractor::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
